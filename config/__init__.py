"""Project-wide lightweight config module.

Some converter/parser modules import `config.config` for verbosity flags and
helper logging. In the monorepo layout, that module is expected at repo root.
"""

