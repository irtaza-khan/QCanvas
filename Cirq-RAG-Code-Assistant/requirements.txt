# Back-compat alias.
#
# The requirements have been split into three layered files:
#   - requirements-base.txt  -> production runtime (FastAPI + Bedrock + pgvector)
#   - requirements-local.txt -> base + FAISS/torch (local-dev parity)
#   - requirements-dev.txt   -> local + tests, lints, notebooks, research libs
#
# The production Docker image installs requirements-base.txt directly.
# Running `pip install -r requirements.txt` installs the full dev bundle for
# backwards compatibility with existing setup scripts.

-r requirements-dev.txt
